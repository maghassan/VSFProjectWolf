<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ng.com.androidlife.vsfproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:debuggable="false"
        android:allowBackup="true"
        android:icon="@drawable/vsf"
        android:label="@string/app_name"
        android:roundIcon="@drawable/vsf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuScreen" />
        <activity android:name=".HouseHold" />
        <activity android:name=".KeyInformants" />
        <activity android:name=".FocusGroup" />
        <activity android:name=".Help"></activity>
    </application>

</manifest>